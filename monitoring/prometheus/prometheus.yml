global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "life-backend"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["localhost:8080"]
        labels:
          service: "life-backend"
          env: "development"
