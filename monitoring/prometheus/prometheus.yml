global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "life-backend"
    metrics_path: "/metrics"
    static_configs:
      - targets:
          - "backend-api:8080"
        labels:
          service: "life-backend"
